# DigestPipeline ApplicationSet entry — register into nanohype/eks-gitops at
# applicationsets/apps-tenants.yaml. Matrix generator: clusters × this list.

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: tenant-digest-pipeline
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - clusters:
              selector:
                matchLabels:
                  argocd.argoproj.io/secret-type: cluster
          - list:
              elements:
                - name: digest-pipeline
                  path: chart
                  syncWave: '100'
  template:
    metadata:
      name: 'digest-pipeline-{{ index .metadata.labels "environment" }}'
      annotations:
        argocd.argoproj.io/sync-wave: '{{`{{syncWave}}`}}'
    spec:
      project: tenant-protohype
      sources:
        - repoURL: https://github.com/nanohype/digest-pipeline.git
          targetRevision: main
          path: '{{`{{path}}`}}'
          helm:
            valueFiles:
              - $values/{{`{{path}}`}}/values.yaml
              - $values/{{`{{path}}`}}/values-{{ index .metadata.labels "environment" }}.yaml
        - repoURL: https://github.com/nanohype/digest-pipeline.git
          targetRevision: main
          ref: values
      destination:
        server: '{{`{{server}}`}}'
        namespace: tenants-protohype
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false # Platform reconciler owns the Namespace
          - ServerSideApply=true
