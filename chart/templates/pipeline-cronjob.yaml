{{- if .Values.pipeline.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "tenant-chart-base.fullname" . }}-pipeline
  labels:
    {{- include "tenant-chart-base.labels" . | nindent 4 }}
    digest-pipeline.io/service: pipeline
spec:
  schedule: {{ .Values.pipeline.schedule | quote }}
  concurrencyPolicy: {{ .Values.pipeline.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.pipeline.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.pipeline.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      activeDeadlineSeconds: {{ .Values.pipeline.activeDeadlineSeconds }}
      backoffLimit: 1
      template:
        metadata:
          labels:
            {{- include "tenant-chart-base.labels" . | nindent 12 }}
            digest-pipeline.io/service: pipeline
        spec:
          restartPolicy: Never
          serviceAccountName: {{ include "tenant-chart-base.serviceAccountName" . }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          containers:
            - name: pipeline
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}-{{ .Values.pipeline.imageTagSuffix }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              securityContext:
                {{- toYaml .Values.containerSecurityContext | nindent 16 }}
              env:
                {{- include "digest-pipeline.env" (dict "Values" .Values "service" "pipeline") | nindent 16 }}
                - name: OTEL_SERVICE_NAME
                  value: digest-pipeline-pipeline
              envFrom:
                - secretRef:
                    name: {{ include "tenant-chart-base.fullname" . }}-secrets
              resources:
                {{- toYaml .Values.pipeline.resources | nindent 16 }}
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir: {}
{{- end }}
