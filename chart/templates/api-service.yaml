{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant-chart-base.fullname" . }}-api
  labels:
    {{- include "tenant-chart-base.labels" . | nindent 4 }}
    digest-pipeline.io/service: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digest-pipeline.selectorLabels" (dict "Values" .Values "Release" .Release "Chart" .Chart "service" "api") | nindent 4 }}
{{- end }}
